<IfModule mod_rewrite.c>
    RewriteEngine On

    # Route all non-file and non-directory requests to Laravel.
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]

</IfModule>
